export class Developer {

    // name:string;
    // description:string;
    // constructor(values: Object = {}) {
    // Object.assign(this, values);
    
    constructor(public name:string, public description:string, public authorId: string){

    }

}
